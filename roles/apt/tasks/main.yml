---
- name: Updating Source Listing
  copy:
    src:  "files/sources.list"
    dest: "/etc/apt/sources.list"
  become: True


- name: Updating Local Repository Cache
  apt:
    update_cache: True
  become: True


- name: Upgrading Distribution Packages
  apt:
    upgrade: dist
  become: True

