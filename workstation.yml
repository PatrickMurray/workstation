---
- hosts:
    - localhost
  tasks:
    - name: Setup APT
      include_role:
        name: apt


    - name: Setup Intel Wireless Firmware
      include_role:
        name: firmware-iwlwifi


    - name: Setup Resilio Sync
      include_role:
        name: resilio-sync

